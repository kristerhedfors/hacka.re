At each time during testing when a screenshot is taken, save together with the png file a markdown file with the same name dot md, containing headers with test file, test name and also content in text and whatever else, provided as debug info for the developer.  Create one script view_screenshots.sh which creates an HTML file from the png and md files where they can be scrolled horizontally, with headings and subjheadings from these snippets
