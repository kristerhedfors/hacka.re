/**
 * A simple function that returns a number
 * @description Returns a fixed number value
 * @returns {Object} IMPORTANT: Always return an object, not a primitive value.
 *                   Returning a primitive value may cause issues with tool calling.
 */
function zzz() {
  // IMPORTANT: Always return an object, not a primitive value like 123
  // Returning a primitive value may cause issues with tool calling
  return {
    result: 999,
    success: true
  };
}
