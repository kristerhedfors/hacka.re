# Authentication Examples Requirements
# Uses only libsodium (PyNaCl) for cryptographic primitives

# Core cryptography - libsodium Python bindings
pynacl>=1.5.0

# Web server framework for examples
starlette>=0.27.0
uvicorn>=0.20.0

# HTTP client for testing
httpx>=0.24.0

# Environment configuration
python-dotenv>=1.0.0

# Basic HTTP requests (fallback)
requests>=2.28.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Alternative web frameworks (optional examples)
flask>=2.3.0
gunicorn>=20.1.0